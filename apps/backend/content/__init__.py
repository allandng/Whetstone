"""Static learning content: the beginner course and the built-in problem bank.

These modules are data, not behavior — plain Python lists of dicts that the
practice router serves (course) or seeds into the database (problems). They
live in the repo so the content is versioned and reviewable like code.
"""
